{
  "name": "codex-boss",
  "version": "0.3.0",
  "private": true,
  "description": "Bridge de memoria semantica do time para o Codex CLI (conecta no servidor MCP via HTTP).",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "doctor": "node scripts/doctor.mjs",
    "doctor:json": "node scripts/doctor.mjs --json",
    "test": "node scripts/selftest.mjs && node scripts/selftest-scope.mjs"
  },
  "license": "MIT",
  "author": "Allan Santos"
}
